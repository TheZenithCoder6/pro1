/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ["images.unpublish.com","images.prod.favotrip.nl","images.unplash.com"],
    }
}

module.exports = nextConfig;